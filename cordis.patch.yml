# The @dsh-community/git-commit host bundle layer: one row that registers
# the commit-pipeline service, the model tool, and the slash commands. The
# browser half is its own bundle layer (see packages/ui-git-commit/cordis.patch.yml),
# registered separately by 'dsh plugin add ./packages/ui-git-commit'.
- insert:
    - id: git-commit
      name: '@dsh-community/git-commit'